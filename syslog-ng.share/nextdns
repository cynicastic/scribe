 # put NextDNS logs into /opt/var/log/NextDNS.log
 
destination d_nextdns {
  file("/opt/var/log/NextDNS.log");
};

filter f_nextdns {
  program("nextdns");
};
 
log {
  source(src);
  filter(f_nextdns);
  destination(d_nextdns);
  flags(final);
};

#eof
