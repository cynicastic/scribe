# don't log empty messages from kernel

destination d_null { 
    file("/dev/null");
};

filter f_blank {
    program("kernel") and
    message("^ *$");
};

log {
    source(src);
    filter(f_blank);
    destination(d_null);
    flags(final);
};

#eof
